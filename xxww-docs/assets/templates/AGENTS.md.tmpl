# AGENTS.md

<!-- 语言偏好，可留可删 -->
使用中文和我沟通。

> **{{PROJECT_NAME}}**：基于当前代码清单维护的项目。修改前先确认项目定位与事实源。
> 根级 `CLAUDE.md` 只包含 `@AGENTS.md`；完整规则只维护在本文件。

## 0. 文档职责

本文件只保留项目定位、最高红线、入口命令和文档索引：

- `docs/boundaries.md` — 目录职责、依赖方向、接缝与完整项目树
- `docs/architecture.md` — 运行蓝图、数据所有权和不变量
- `docs/errors.md` — 已发生误判的根因与预防
- `docs/decisions.md` — 长期架构决策

**铁律：项目树先行。** 新增/删除/移动任何目录、workspace、模块边界或接缝，必须在同一变更序列内先更新 `docs/boundaries.md` 的完整项目树（及本文 §1 顶层地图），再动代码；不先对齐项目树的改动一律视为越界。

**铁律：错误入册。** 每次犯错、误判或返工后，必须在 `docs/errors.md` 追加一条（现象/根因/修复/预防），最新在上；不写进代码注释、不散落各文档，目的是同一个坑不踩第二次。

## 1. 顶层地图

```text
{{PROJECT_TREE}}
```

完整逐目录边界见 `docs/boundaries.md`。

## 2. 最高红线

1. 不提交密钥、凭证明文、生成产物或本机配置。
2. 不绕过公开接口制造反向依赖；先从 composition root 和 manifest 确认依赖方向。
3. 有副作用的操作必须说明幂等、失败处理与回滚路径。
4. demo、fallback、计划或源码存在不得写成生产完成态。
5. **单个代码文件不超过 1000 行**（按物理行数计）；逼近上限先按职责拆分模块，不靠压行、删注释或合并语句绕过。生成物、vendored 代码与快照除外。

## 3. 启动与验证

```bash
{{COMMANDS}}
```

## 4. 文档索引

- `docs/boundaries.md` — 目录与依赖边界
- `docs/architecture.md` — 架构与数据所有权
- `docs/errors.md` — 错误记录
- `docs/decisions.md` — 决策记录
