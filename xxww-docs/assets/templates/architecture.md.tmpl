# 架构蓝图

> 状态：active
> 生成于 {{GENERATED_DATE}}；只保留从当前代码验证过的运行关系。

## 1. 系统上下文

根据应用入口、部署配置和外部依赖绘制运行蓝图。不要从目录名猜测运行关系。

## 2. 数据所有权

列出核心数据、唯一事实源、读写方、持久化位置和敏感性边界。

## 3. 不变量

记录必须跨实现保持成立的安全、事务、兼容和失败处理规则。

## 4. 技术栈

只记录 manifest、lockfile 或工具链配置能验证的技术与版本。
